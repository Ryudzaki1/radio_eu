# Scripts

This folder keeps only current project scripts at the top level.

- `smoke-tests.js` - fast local/container smoke checks.
- `ops/` - reusable operational scripts for checking or deploying the current RU/EU setup.
