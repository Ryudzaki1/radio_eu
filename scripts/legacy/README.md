# Legacy scripts

One-off diagnostics, bootstrap helpers, and historical repair scripts.

They are kept to preserve context, but they are not part of the normal deploy or
runtime flow. Prefer `scripts/ops/` for current operations.
